import propValidator from '../utils/propValidator.mjs';
import BuildClass from './buildClass.mjs';

/**
 * #Size Mixin/Interface
 *
 * Functionality for handling/standardizing component sizing.
 *
 *  :Values
 *    :small
 *    :medium (default)
 *    :large
 *
 * @mixin
 */
var size = {
  mixins: [BuildClass],
  props: {
    /**
     * Size state
     */
    size: {
      type: String,
      default: 'medium',
      validator: function validator(value) {
        return propValidator(value, ['small', 'medium', 'large']);
      }
    }
  },
  computed: {
    sizeClass: function sizeClass() {
      var returnClass = this.buildClass('size');
      return returnClass;
    }
  }
};

export default size;
